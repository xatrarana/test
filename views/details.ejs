<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=data.title%>
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">

</head>

<body class="flex">
    <%- include('sidebar') %>
        <div class="flex-1">
            <%- include('header') %>
                <div class="workspace-area ">
                    <div class="p-3">

                        <div class="palcedetils">
                            <div class="max-w-xl mx-auto">
                                <div class="bg-white p-6 rounded shadow-lg">
                                    <div>
                                        <h1 class="text-2xl font-bold mb-4">Place Details</h1>

                                    </div>
                                    <div class="grid grid-cols-2 gap-4">
                                        <div class="flex items-center">
                                            <strong class="mr-2">Title:</strong>
                                            <%= data.title %>
                                        </div>
                                        <div class="flex items-center">
                                            <strong class="mr-2">Slug Name:</strong>
                                            <%= data.slug_name %>
                                        </div>
                                        <div class="flex items-center">
                                            <strong class="mr-2">Description:</strong>
                                            <%= data.description %>
                                        </div>
                                        <div class="flex items-center">
                                            <strong class="mr-2">Location:</strong>
                                            <%= data.location %>
                                        </div>
                                        <div class="flex items-center">
                                            <strong class="mr-2">Ward No:</strong>
                                            <%= data.wardno %>
                                        </div>
                                        <div class="flex items-center">
                                            <strong class="mr-2">Category:</strong>
                                            <%= data.category %>
                                        </div>
                                        <div class="flex items-center">
                                            <strong class="mr-2">Coordinates:</strong>
                                            <%= data.coordinates.coordinates.join(', ') %>
                                    </div>
                                    <div class="flex items-center">
                                        <strong class="mr-2">Total Rating:</strong> <%= data.totalRating %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        </div>
                        
                      
                      
                      </div>

                </div>

        </div>
        
</body>

</html>